App\Entity\Doctrine\DepotEntity:
    type: entity
    table: depots
    uniqueConstraints:
        id_UNIQUE:
            columns:
                - id
    id:
        id:
            type: integer
            nullable: false
            options:
                unsigned: true
                comment: 'Numéro VME'
            id: true
            generator:
                strategy: IDENTITY
    fields:
        name:
            type: string
        open:
            type: boolean
    lifecycleCallbacks: {  }
