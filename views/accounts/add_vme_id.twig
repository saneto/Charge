<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, height=device-height">

    <title>Avant de continuer</title>
    <base href="{{ base_url() }}">

    <link rel="icon" href="favicon.ico">
    <link rel="stylesheet" href="css/errordoc.css">

    <style type="text/css">
        body { font-family: tktype, sans-serif; }
    </style>
</head>
<body>
    <header id="header">
        <h1>thyssenkrupp Materials France</h1>
        <p>Merci de prendre un peu de temps avant de continuer à utiliser {{ app }} :)</p>
    </header>

    <p class="details">
        {{ user }},<br><br>

        Afin de poursuivre dans l'application {{ app }},<br>
        merci de renseigner votre code vendeur VME (dans le formulaire juste en dessous).
    </p>

    <div>
        <form action="" method="post">
            <input type="hidden" name="_METHOD" value="PUT">
            <label for="vme_id">Mon code vendeur VME:</label>

            <p>
                <input type="text" id="vme_id" name="vme_id" value="{{ user.vmeId }}" placeholder="Votre code vendeur ici" maxlength="2" required>
                <button type="submit">OK !</button>
            </p>
        </form>

        {% for type, flashes in flash %}
            {% for f in flashes %}
                <p>{{ f }}</p>
            {% endfor%}
        {% endfor%}
    </div>
</body>
</html>